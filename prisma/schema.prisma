generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model Entry {
  id        String   @id @default(cuid())
  name      String
  unit      Int
  cardType  CardType
  createdAt DateTime @default(now())
}

enum CardType {
  SFY
  AAY
  PHH
}